# Лабораторная работа № 7. Работа с СУБД

Вариант 2, средний уровень:

- № 2 — вставка данных в таблицу SQLite;
- № 4 — обновление записи;
- № 7 — создание связанных таблиц `groups` и `students`.

Запросы параметризованы, включены внешние ключи. Демонстрационный запуск
создаёт `lab07.db`; файл базы исключён из Git.

```bash
python -m lab07.database
```
