# Лабораторная работа № 7. Работа с СУБД

Вариант 2, средний уровень:

- `task_02.py` — вставка данных в таблицу SQLite;
- `task_04.py` — обновление записи;
- `task_07.py` — создание связанных таблиц `groups` и `students`.

Скрипты создают учебные базы `students.db` и `university.db`. Файлы баз
исключены из Git.

```bash
python -m lab07.task_02
python -m lab07.task_04
python -m lab07.task_07
```
