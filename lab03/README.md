# Лабораторная работа № 3. ООП

Вариант 2, средний уровень:

- № 2 — класс `Student` с методом вывода информации;
- № 4 — абстрактный класс `Shape`, классы `Rectangle` и `Circle` с методами
  вычисления площади и периметра;
- № 7 — класс `Vector2D` с перегруженным оператором `+`.

```bash
python -m lab03.models
```
