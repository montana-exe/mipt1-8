# Лабораторная работа № 3. ООП

Вариант 2, средний уровень:

- `task_02.py` — класс `Student` с методом вывода информации;
- `task_04.py` — класс `Shape` с вычислением площади и периметра;
- `task_07.py` — класс `Vector` с перегруженным оператором `+`.

```bash
python -m lab03.task_02
python -m lab03.task_04
python -m lab03.task_07
```

В каждом файле есть отдельный пример создания объектов и пояснения к строкам.
