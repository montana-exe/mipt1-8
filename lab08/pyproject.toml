[tool.poetry]
name = "mitp-lab08"
version = "1.0.0"
description = "Лабораторная работа № 8, вариант 2"
authors = ["Студент"]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.11,<4.0"
requests = "^2.32"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"

[build-system]
requires = ["poetry-core>=1.9.0"]
build-backend = "poetry.core.masonry.api"
