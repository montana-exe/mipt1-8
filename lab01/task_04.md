# Задание № 4. Изменение файла и второй коммит

В `main.py` функция приветствия была изменена: она получила параметр `name` и
стала возвращать персонализированный текст.

```bash
git add lab01/main.py
git commit -m "feat(lab1): update greeting"
```

Первая команда подготавливает изменённую версию файла. Вторая создаёт новый
снимок проекта, не удаляя предыдущий. Коммит имеет идентификатор `5b11ee2`.
