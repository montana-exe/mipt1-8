# Лабораторная работа № 2. Структурное программирование

Вариант 2, средний уровень:

- № 2 — сумма всех нечётных чисел до `N` включительно;
- № 4 — НОД двух чисел;
- № 7 — подсчёт гласных в строке.

```bash
python -m lab02.tasks
```

Программа проверяет пользовательский ввод и поддерживает отрицательные числа
при вычислении НОД. Русские гласные `ё`, `ы`, `э`, `ю`, `я` учитываются.
