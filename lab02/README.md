# Лабораторная работа № 2. Структурное программирование

Вариант 2, средний уровень:

- `task_02.py` — сумма всех нечётных чисел до `N` включительно;
- `task_04.py` — НОД двух чисел;
- `task_07.py` — подсчёт гласных в строке.

```bash
python -m lab02.task_02
python -m lab02.task_04
python -m lab02.task_07
```

Каждый файл запускается отдельно и содержит только последовательный код задачи.
